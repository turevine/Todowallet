# Admin panel (/admin) — copy to .env.local and fill in.
# Never commit .env.local.

ADMIN_EMAIL=ceo@todowallet.net
ADMIN_PASSWORD=
# Long random string (e.g. openssl rand -hex 32)
ADMIN_SESSION_SECRET=
